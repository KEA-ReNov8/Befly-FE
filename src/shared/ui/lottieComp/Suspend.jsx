import Lottie from 'lottie-react';
import suspend from '@shared/assets/lotties/sad.json';

const Suspend = () => {
    return <Lottie animationData={suspend} loop={true} />;
};

export default Suspend;
